"use strict" // treat all js code as a newer version

// alert ( 3 + 3 ) //it is a nodejs not a browser

console.log( 3
    + 3)   //it is not good format of written code

   
console.log("Nikhil") //code readability should be high

let name="Nikhil"         //String
let age = 20             //Number
let isloggedIn = false  //Boolean datatype

let state;

// number => 2 to ppower 53 (range)
// bigint (it is use for assign big value like facebook. It use bigint because facebook stores data of many people)
// string => ""
// boolean => true/false
// null => standalone value (its typeof is object)
// undefined => 
// symbol => unique

//object

console.log(typeof undefined);
console.log(typeof null);
console.log(typeof "Nikhil")
console.log(typeof age);
console.log(typeof true);